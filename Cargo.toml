[package]
name = "erh_llm"
version = "0.1.0"
edition = "2024"

[dependencies]
ollama-rs = "0.3"
log = "0.4"
rusqlite = { version = "0.37", optional = true }
uuid = { version = "1.17", optional = true }

[features]
default = ["mem_hist"]
sqlite_hist = ["rusqlite", "uuid"]
mem_hist = []